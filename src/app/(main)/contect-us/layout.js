import React from "react";

export const metadata = {
  title: "تماس با ما",
  description: "تماس با ما",
};
export default async function layoutContact({ children }) {
  return <div>{children}</div>;
}
