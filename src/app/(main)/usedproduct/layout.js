import React from "react";

export const metadata = {
  title: "دست دوم کاربران",
  
};

export default async function layoutUserAdd({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
