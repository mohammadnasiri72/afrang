import React from "react";

export const metadata = {
  title: "کالاهای دسته دوم",
  description: "کالاهای دسته دوم",
};

export default async function layoutUserAdd({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
