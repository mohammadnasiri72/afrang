import React from "react";

export const metadata = {
  title: "لیست قیمت ها",
  description: "لیست قیمت ها",
};
export default async function layoutPriceList({ children }) {
  return <div>{children}</div>;
}
